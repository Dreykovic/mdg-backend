// Maximum file size limit set to 50 MB (in bytes)
export const LIMIT_FILE_SIZE = 1024 * 1024 * 50; // 50 MB

// List of allowed file types by their extensions
export const ALLOWED_FILE_TYPES = ['jpg', 'jpeg', 'png', 'pdf', 'mp4', 'mp3'];

// List of allowed MIME types to validate file content
export const MIME_TYPES = ['image/', 'application/pdf', 'video/', 'audio/'];
